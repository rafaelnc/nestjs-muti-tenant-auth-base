{
  "name": "reservahora-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "turbo run dev",
    "build": "turbo run build --global-deps='.env'",
    "start": "node apps/backend/dist/main",
    "debug": "node --nolazy --inspect-brk=5858 run dev"
  },
  "workspaces": [
    "apps/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^1.13.2"
  },
  "dependencies": {
    "nest": "^0.1.6"
  }
}
